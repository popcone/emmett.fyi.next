import { Red_Hat_Text, Open_Sans } from 'next/font/google'

export const openSans = Open_Sans({
  variable: '--font-open-sans',
  subsets: ['latin'],
})

export const redHatText = Red_Hat_Text({
  variable: '--font-red-hat-text',
  subsets: ['latin'],
})
